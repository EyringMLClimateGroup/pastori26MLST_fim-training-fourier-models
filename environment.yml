name: envfimfourier
channels:
  - conda-forge
dependencies:
  - python
  - numpy
  - scipy
  - scikit-learn
  - jax
  - optax
  - pennylane
  - autoray<0.7  # newer autoray removed NumpyMimic, which pennylane.numpy still imports
  - matplotlib
  - jupyter
  - ipykernel
